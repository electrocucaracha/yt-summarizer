{"category": "Changed", "changelog_entry": "Updated dependencies to Python 3.13, affecting the `aiohappyeyeballs` and `aiohttp` packages with numerous wheel files updated for the new Python version.", "summary": "Dependencies were updated in the `uv.lock` file, changing the required Python version from 3.12 to 3.13 and updating the revision number to 1. The update affects the `aiohappyeyeballs` and `aiohttp` packages, with the latter having a large number of wheel files updated for Python 3.13.", "version": 1}
