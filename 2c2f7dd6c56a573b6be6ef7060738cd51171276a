{"category": "Added", "changelog_entry": "Optimized the YouTube summarizer service to update only modified records, resulting in improved performance and reduced database load.", "summary": "The YouTube summarizer service now only updates records that have been modified, improving efficiency by reducing unnecessary updates. This change affects the service's behavior, as it will only update videos that have been changed since the last update. The API contract remains unchanged, and there are no security implications or config schema changes. The primary user outcome is improved performance and reduced database load.", "version": 1}
