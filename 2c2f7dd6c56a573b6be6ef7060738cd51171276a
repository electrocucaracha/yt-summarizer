{"category": "Added", "changelog_entry": "Optimized the service to update only modified records, reducing unnecessary database updates and introducing a new API change where the `updated` flag is set to `True` when a video's title, summary, or main points are fetched or generated.", "summary": "The service now only updates records that have been modified, improving efficiency by reducing unnecessary database updates. This change does not break any behavior or require migration, but it does introduce a new API change: the `updated` flag is set to `True` when a video's title, summary, or main points are fetched or generated. The `update_video` method now only updates videos with `updated=True`.", "version": 1}
