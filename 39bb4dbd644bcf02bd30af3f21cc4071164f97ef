{"category": "Fixed", "changelog_entry": "The update workflow now securely updates the Dockerfile in the check-dockerfile job using the WORKFLOW_TOKEN, replacing the need for the GITHUB_TOKEN.", "summary": "The update workflow now passes the WORKFLOW_TOKEN to PR-creation actions, replacing the GITHUB_TOKEN. This change affects the update workflow's execution, particularly in the check-dockerfile job, where the token is used to update the Dockerfile. The WORKFLOW_TOKEN is stored as a secret, ensuring secure access to the token. This change requires no migration steps.", "version": 1}
