{"category": "Fixed", "changelog_entry": "Resolved the issue of creating pull requests by passing the WORKFLOW_TOKEN to PR-creation actions in the update workflow, requiring no migration steps.", "summary": "The GitHub workflow for updating the project was modified to use the WORKFLOW_TOKEN instead of the GITHUB_TOKEN when creating pull requests. This change affects the environment variables used in the workflow and requires no migration steps. The update also includes changes to the permissions and token used in the check-dockerfile job.", "version": 1}
