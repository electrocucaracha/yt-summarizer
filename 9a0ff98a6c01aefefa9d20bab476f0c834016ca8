{"category": "Added", "changelog_entry": "Enabled users to package the application into a container for easier deployment with the addition of a Dockerfile, which minimizes dependencies and includes environment variables for configuration.", "summary": "A Dockerfile has been added to the project, allowing users to package the application into a container for easier deployment. The Dockerfile uses a multi-stage build to minimize dependencies and includes environment variables for configuration. This change requires no migration steps, and there are no breaking API or CLI changes. The security impact is minimal, and the primary user outcome is improved deployment flexibility.", "version": 1}
