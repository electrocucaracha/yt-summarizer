{"category": "Added", "changelog_entry": "Enabled translation features by introducing googletrans and updating multiple dependencies to newer versions, including httpx, h2, hpack, and hyperframe.", "summary": "The main dependencies in `pyproject.toml` were updated to newer versions, including `googletrans` for translation features. Several other dependencies were also updated, such as `litellm`, `huggingface-hub`, `markdown-it-py`, and `fsspec`. The `uv.lock` file was updated to reflect the new version of `aiohttp`. These changes ensure compatibility with the new translation feature and keep the environment up to date with upstream improvements and security fixes.", "version": 1}
