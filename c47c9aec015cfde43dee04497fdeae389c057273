{"category": "Changed", "changelog_entry": "Updated the project's dependencies to use the latest available versions.", "summary": "Versions of several dependencies were updated in the project's configuration files. The changes include updating the `actions/checkout` action to version 6.0.2, the `astral-sh/setup-uv` action to version 7.3.0, and the `actions/setup-python` action to version 6.2.0. Additionally, the `docker/login-action` and `docker/metadata-action` actions were updated to versions 3.7.0 and 5.10.0, respectively. The `docker/build-push-action` and `actions/attest-build-provenance` actions were also updated to versions 6.19.2 and 3.2.0, respectively.", "version": 1}
