{"category": "Changed", "changelog_entry": "The update workflow now uses the github.token environment variable for creating pull requests, replacing the need for a WORKFLOW_TOKEN secret.", "summary": "The GitHub workflow for updating the project now uses the `github.token` environment variable for creating pull requests, replacing the `WORKFLOW_TOKEN` secret. This change affects the behavior of the update workflow and may require adjustments to the workflow token management. The update process itself remains unchanged.", "version": 1}
