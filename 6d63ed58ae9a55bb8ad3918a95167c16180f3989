{"category": "Changed", "changelog_entry": "Updated the update workflow to use the `github.token` for creating pull requests, eliminating the need for the `WORKFLOW_TOKEN` secret.", "summary": "The GitHub workflow for updating the project was modified to use the `github.token` for creating pull requests, replacing the use of the `WORKFLOW_TOKEN` secret. This change affects the creation of pull requests in the update workflow, and users and maintainers should note the updated token usage. The migration to `github.token` is a breaking change, requiring no additional steps.", "version": 1}
