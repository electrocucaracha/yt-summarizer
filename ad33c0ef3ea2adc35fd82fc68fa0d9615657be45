{"category": "Added", "changelog_entry": "Enabled automated pre-commit checks for the repository, enforcing code quality and consistency before changes are committed, with tools for linting, formatting, and generating commit messages.", "summary": "Automated pre-commit checks are now enabled for the repository, enforcing code quality and consistency before changes are committed. The configuration includes hooks for linting, formatting, and generating commit messages, reducing manual review effort and minimizing errors. This change introduces no breaking behavior or migration requirements, and does not affect the API or CLI contract. The configuration schema has been updated with new tools.", "version": 1}
