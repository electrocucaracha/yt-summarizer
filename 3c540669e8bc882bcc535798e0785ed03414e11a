{"category": "Added", "changelog_entry": "Enabled improved performance and reliability when fetching page properties from Notion databases by utilizing the data source query endpoint when available.", "summary": "The Notion client now fetches pages from databases using the data source query endpoint when available, improving performance and reducing the load on the Notion API. This change breaks behavior for databases without a data source, which will now fall back to the previous method. The API contract remains unchanged, but the internal implementation has been updated to handle data source pagination and fallback scenarios. The primary user outcome is improved performance and reliability when fetching page properties from Notion databases.", "version": 1}
