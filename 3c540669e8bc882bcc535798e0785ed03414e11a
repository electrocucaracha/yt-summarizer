{"category": "Added", "changelog_entry": "Optimized fetching pages from Notion databases by leveraging the data source query endpoint when available, reducing the number of API calls and increasing efficiency without altering the API contract or requiring migration steps.", "summary": "The Notion database fetching functionality has been improved to use the data source query endpoint when available, reducing the number of API calls and increasing efficiency. This change affects the `get_page_properties_from_database` method, which now uses the data source query when a database has a data source, and falls back to the previous behavior when no data source is present. The API contract remains unchanged, and no migration steps are required.", "version": 1}
