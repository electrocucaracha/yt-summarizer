{"category": "Added", "changelog_entry": "Introduced the ability to store the Notion API token in a file, allowing users to provide their token through either a file or the NOTION_TOKEN environment variable.", "summary": "The Notion API token is now read from a file instead of being set as an environment variable. This change affects how users provide their Notion API token, with the option to either create a token file or use the NOTION_TOKEN environment variable. The CLI and Docker configurations have been updated to reflect this change, and the token file path can be overridden with the --notion-token-file option.", "version": 1}
