{"category": "Added", "changelog_entry": "Introduced a new configuration option that allows users to specify a custom Notion API token file location, requiring a valid token file for the application to function correctly.", "summary": "The Notion API token is now read from a file instead of being hardcoded or set via environment variable. This change adds a new configuration option, `--notion-token-file`, which allows users to specify a custom token file location. The default location is `/etc/notion/secrets.txt`, and users can override this by setting the `NOTION_TOKEN_FILE` environment variable. This change does not break any existing behavior, but users must now provide a valid token file for the application to function correctly.", "version": 1}
