{"category": "Added", "changelog_entry": "Enabled the summarizer to process YouTube playlists by introducing a new CLI option to specify the playlist URL to process.", "summary": "The `playlist processing` feature has been implemented, allowing the summarizer to process YouTube playlists and update the Notion database with video summaries. This change introduces a new CLI option `--playlist-url` to specify the playlist URL to process. The `process_playlist` method in the `YouTubeSummarizerService` class has been added to handle playlist processing, which fetches video metadata and generates summaries for each video in the playlist.", "version": 1}
