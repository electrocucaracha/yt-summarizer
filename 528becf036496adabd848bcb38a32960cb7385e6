{"category": "Removed", "changelog_entry": "Optimized the build process by switching to unpinned packages for gcc and musl-dev, improving compatibility with upstream images and reducing maintenance overhead, but potentially introducing minor differences in build environments.", "summary": "Pinning gcc and musl-dev versions in the Dockerfile caused build failures due to unavailable versions in newer Alpine repositories. Removing these version pins improves compatibility with upstream images and reduces maintenance overhead, but may introduce minor differences in build environments. This change ensures smoother CI and local builds by allowing the build process to adapt to changing package versions.", "version": 1}
