{"category": "Fixed", "changelog_entry": "Skip artifact attestation on pull requests to avoid unnecessary checks.", "summary": "Artifact attestation is now skipped on pull requests to avoid unnecessary checks. This change affects the GitHub Actions workflow, specifically the \"Generate artifact attestation\" step, which is now conditionally skipped when the event is a pull request. The attestation step remains in place for other events, ensuring provenance is still verified for non-pull requests.", "version": 1}
