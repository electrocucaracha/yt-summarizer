{"category": "Added", "changelog_entry": "Automated Docker image creation and publication is now enabled through a new workflow triggered on pushes to and pull requests to the main branch.", "summary": "A build CI task was added to automate the creation and publication of a Docker image. This change introduces a new workflow that triggers on pushes to the main branch and on pull requests to the main branch, and it includes unit tests and a push to the Docker registry. The task also generates an artifact attestation.", "version": 1}
