{"category": "Fixed", "changelog_entry": "Simplified the YAML linting configuration format to remove an unnecessary line, resulting in a slightly more streamlined configuration file with no breaking changes or migration requirements.", "summary": "The YAML linting configuration has been updated to a more concise format, removing an unnecessary line. This change does not introduce any breaking behavior, API or CLI changes, or security impact. The config schema remains unchanged, and no migration steps are required. The primary user outcome is a slightly simplified configuration file.", "version": 1}
